<!DOCTYPE html>
<html>
  <head>
    <title>WhatAreYouWearing?</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= stylesheet_link_tag    "contents", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <div class="brand">
            <span><%= link_to 'WhatAreYouWearing?', controller: 'costumes', action: 'reset' %></span>
          </div>
          <div class="nth pull-right">
            <span><%= "#{nth} 回目" %></span>
          </div>
        </div>
      </div>
    </header>
    <div class="container">
      <div class="row">
        <%= yield %>
      </div>
      <div class="flash">
        <% flash.each do |key, value| %>
          <%= content_tag(:div, value, class:"alert alert-#{key}") %>
        <% end %>
      </div>
    </div>
  </body>
</html>
