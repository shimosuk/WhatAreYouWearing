<!DOCTYPE html>
<html>
  <head>
    <title>WhatAreYouWearing?</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= stylesheet_link_tag    "contents", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <div class="brand">
            <% if request.path == '/costumes' %>
              <span><%= link_to 'WhatAreYouWearing?', costumes_rule_path %></span>
            <% else %>
              <span><%= link_to 'WhatAreYouWearing?', costumes_path %></span>
            <% end %>
          </div>
          <div class="nth pull-right">
          <span><%= "#{nth} 回目" %></span>
          </div>
        </div>
      </div>
    </header>
    <div class="container">
      <div class="row">
        <%= yield %>
      </div>
      <div class="flash">
        <% flash.each do |key, value| %>
          <%= content_tag(:div, value, class:"alert alert-#{key}") %>
        <% end %>
      </div>
    </div>
  </body>
</html>
